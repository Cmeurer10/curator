<%= simple_form_for(@conversation) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :book %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
