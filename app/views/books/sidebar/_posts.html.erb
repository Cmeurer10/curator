<div id='post-container'>
  <h2>Posts</h2>
  <ul>
    <% @posts.each do |post| %>
      <li>
        <p><%= post.content %></p>
        <p>Posted by: <%= post.user.full_name %> at <%= post.created_at %></p>
      </li>
    <% end %>
  </ul>
  <%= render "/posts/form", locals: {post: @post, book: @book, conversation: @conversation} %>
  <div class="btn btn-primary" id='new-post-btn'>New Post</div>
</div>
