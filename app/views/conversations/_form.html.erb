<% @conversation = Conversation.new %>
<%= simple_form_for([@book, @conversation]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :topic, as: :text %>
  </div>

  <div class="form-actions">
    <%= f.submit "Create New Thread" %>
  </div>
<% end %>
